package initialize

import (
	// "database/sql"
	// "log"
	// "simplebank/configs"
	// "simplebank/internal/util"

	// "gorm.io/driver/postgres"
	// "gorm.io/gorm"
)

func InitPostgreSQL() error {
	// cfg, err := configs.LoadConfig("../../configs", "local")
	// if err != nil {
	// 	log.Fatal("cannot load config:", err)
	// }
	// db, err = gorm.Open(postgres.Open(cfg.DB_URL), &gorm.Config{
	// 	SkipDefaultTransaction: true,
	// })
	// util.CheckPaniceError(err, "failed to connect to database")

	// setPool()
	

	// migrateTable()

	return nil
}

// func setPool() {
// 	sqlDB, err := db.Db()
// }

func migrateTable() {}